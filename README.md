# docker compose ignorante con nginx + php + mariadb
Docker compose per servizi:
- PHP latest
- nginx
- mariadb latest
inclusi in app/ anche due script php ignoranti per mostrare phpinfo() e provare connessione al DB.
